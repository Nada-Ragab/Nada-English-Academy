Nada English Academy V22.4

إصلاحات هذا الإصدار:
- حذف استخراج JSON باستخدام Regex الذي كان يقطع رد Gemini.
- قراءة رد Firebase AI Logic باستخدام JSON.parse مباشرة.
- تنظيف code fences والنص المحيط بكائن JSON فقط.
- محاولة إصلاح الفواصل الزائدة وعلامات الاقتباس الذكية.
- منع عرض أجزاء JSON الخام داخل المحادثة عند تعذر القراءة.
- عرض رسالة عربية واضحة مع تسجيل التفاصيل في Console للمراجعة.

التشغيل:
- ارفعي محتويات المجلد إلى GitHub Pages.
- بعد النشر استخدمي Ctrl + F5 مرة واحدة.
- يفضل التشغيل من https أو localhost وليس file://.
